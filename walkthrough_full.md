"# Phase 3 Walkthrough: Final Dual-Stream Deepfake Detection Model\n\n## Summary of Accomplishments\n\nWe have successfully engineered a state-of-the-art dual-stream deepfake detection pipeline, completely rectifying the English-bias and \"simple synthetic generator\" biases present in the original dataset.\n\n### 1. Advanced ElevenLabs Hindi Dataset Generation\nWe leveraged the ElevenLabs API to generate **100 native, high-fidelity Hindi audio clips** with realistic variations (Formal, Conversational, Urgent) focusing heavily on Banking and KYC fraud scripts.\n- To bypass API tier limitations for uncompressed audio, we intercepted high-quality MP3 streams and transcoded them natively in memory to `16-bit PCM WAV` using `soundfile` and `librosa`.\n- All generated audio was saved directly to the workspace in `.wav` format, fully playable within Windows natively.\n\n### 2. The Tiered Degradation Engine\nTo ensure the model learns biological signatures (breathing, prosody, mucosal dynamics) rather than just the \"clean encoding fingerprint\" of ElevenLabs, we layered our 100 base clips into **300 diverse samples**:\n1. **Clean Layer**: Pristine `16kHz` ElevenLabs outputs.\n2. **Codec Layer**: 8kHz downsampled telephonic simulation.\n3. **Noise Layer**: Additive gaussian background noise (SNR 5-20dB).\n4. **Replay Layer**: Simulation of the audio being played out of a loudspeaker and re-recorded on a phone microphone (bandpass + reverb).\n\n### 3. Dual-Stream Fusion Training\nWe extracted two distinct streams of features across the massive **6,666-sample** multi-lingual dataset:\n- **Bio Stream (`extract_bio.py`)**: 345-dimensional acoustic vector capturing micro-tremors, pitch stability, and formant ratios.\n- **Deep Stream (`extract_deep.py`)**: 768-dimensional temporal neural embeddings using Facebook's `wav2vec2-base`.\n\nWe then trained two independent `XGBoost` classifiers and dynamically fused their output probabilities.\n\n---\n\n## Final Performance Metrics\n\nOur final validation and testing yielded spectac
<truncated 1040 bytes>